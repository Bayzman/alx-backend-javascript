export default function createEmployeesObject(departmentName, employees) {
    // const output = {
    //     [ `${departmentName}` ]: employees,
    // };
    return {
        [ `${departmentName}` ]: employees,
    };
}
